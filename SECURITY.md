# Security Policy

If you discover a security issue (e.g., an insecure default, exposed credential, or vulnerable configuration),
please **do not** open a public issue. Email the maintainer instead.

- Do not commit secrets. The workflow is designed to build without any repository secrets.
- SSH defaults are enabled, but first-boot requires setting a user and password unless you switch to key-only auth.
